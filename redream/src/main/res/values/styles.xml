<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="publicloadingProgressStyle">
        <item name="android:maxWidth">150dip</item>
        <item name="android:maxHeight">220dip</item>
        <item name="android:minWidth">150dip</item>
        <item name="android:minHeight">220dip</item>
    </style>

    <style name="myStateBarTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <!-- toolbar（actionbar）颜色 -->
        <item name="colorPrimary">#673AB7</item>
        <!-- 状态栏颜色 -->
        <item name="colorPrimaryDark">@color/shenhui</item>
    </style>
</resources>
